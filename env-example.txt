# ============================================
# CONFIGURACIÓN DE BASE DE DATOS
# ============================================

# Tipo de base de datos (mysql | postgres)
DB_TYPE=mysql

# Host de la base de datos
DB_HOST=mysql

# Puerto de la base de datos
DB_PORT=3306

# Usuario de la base de datos
DB_USER=zigo_user

# Contraseña de la base de datos
DB_PASSWORD=root123

# Nombre de la base de datos
DB_NAME=zigo_ecommerce

# ============================================
# CONFIGURACIÓN DE LA APLICACIÓN
# ============================================

# Puerto en el que corre el backend
BACKEND_PORT=3000

# Entorno de ejecución (development | production)
NODE_ENV=development

# Nombre de la aplicación
APP_NAME="E-Commerce API"

# URL base de la aplicación
APP_URL=http://localhost:3000

# ============================================
# CONFIGURACIÓN DE JWT (REQUERIDO)
# ============================================

# Secret key para firmar los tokens JWT
# IMPORTANTE: Esta variable es REQUERIDA. La aplicación NO iniciará sin ella.
# Genera una clave segura con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production_minimum_32_characters

# Tiempo de expiración del token (ej: 15m, 1h, 1d, 7d)
JWT_EXPIRATION=1d

# Tiempo adicional para renovación automática del token
JWT_RENEWAL_EXTENSION=10m

# Secret key para firmar refresh tokens (debe ser distinta al JWT_SECRET)
JWT_REFRESH_SECRET=your_super_secret_refresh_key

# ============================================
# CONFIGURACIÓN DE REDIS
# ============================================

# Host de Redis
REDIS_HOST=redis

# Puerto de Redis
REDIS_PORT=6379

# Contraseña de Redis (opcional)
# REDIS_PASSWORD=your_redis_password

# Base de datos de Redis (0-15)
REDIS_DB=0

# Tiempo de vida (TTL) de los tokens en blacklist (en segundos)
REDIS_TTL=86400

# ============================================
# CONFIGURACIÓN DE FRONTEND (opcional)
# ============================================

# Puerto donde se expone el frontend (solo si usas docker-compose)
FRONTEND_PORT=80

# URL del frontend para CORS (solo para desarrollo local)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:4200

# ============================================
# CONFIGURACIÓN DE FRONTEND - VITE (opcional)
# ============================================

# URL base de la API para el frontend (usada por Vite en build)
VITE_API_URL=http://localhost:3000/api

# ============================================
# CONFIGURACIÓN DE BCRYPT
# ============================================

# Rondas de encriptación para contraseñas
BCRYPT_ROUNDS=10

# ============================================
# CONFIGURACIÓN DE ARCHIVOS/IMÁGENES
# ============================================

# Tamaño máximo de archivo en bytes (5MB = 5242880)
MAX_FILE_SIZE=5242880

# Directorio donde se suben los archivos
UPLOAD_DIR=./uploads

# URL base para acceder a los archivos subidos
UPLOAD_URL=http://localhost:3000/uploads

# ============================================
# CONFIGURACIÓN DE INVENTARIO
# ============================================

DEFAULT_MIN_STOCK=10
DEFAULT_MAX_STOCK=1000

# ============================================
# CONFIGURACIÓN DE PEDIDOS
# ============================================

# Costo de envío por defecto
DEFAULT_SHIPPING_COST=25.00

# Tasa de impuesto (ej: 0.12 = 12%)
TAX_RATE=0.12

# Días estimados de entrega
DEFAULT_DELIVERY_DAYS=3

# ============================================
# CONFIGURACIÓN DE SEGURIDAD
# ============================================

# Habilitar Helmet (cabeceras de seguridad)
ENABLE_HELMET=true

# Habilitar throttling (limite de peticiones)
ENABLE_THROTTLING=true

# Intentos máximos de login antes de bloqueo
MAX_LOGIN_ATTEMPTS=3

# ============================================
# INSTRUCCIONES DE USO
# ============================================

# 1. Copia este archivo como .env en la raíz del proyecto:
#    cp env.example .env
# 
# 2. Edita el archivo .env con tus valores reales
# 
# 3. NUNCA subas el archivo .env a Git (ya debe estar en .gitignore)
# 
# 4. Para producción, usa variables de entorno del servidor
